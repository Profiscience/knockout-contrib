import * as ko from 'knockout'
import { Context, IContext } from '@profiscience/knockout-contrib-router'

export function* {{camelCase name}}Middleware(ctx: Context & IContext) {
  /* beforeRender */

  yield
  /* afterRender */

  yield
  /* beforeDispose */

  yield
  /* afterDispose */
}
