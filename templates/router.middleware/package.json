{
  "name": "@profiscience/knockout-contrib-router-middleware-{{kebabCase name}}",
  "version": "1.0.0-alpha.0",
  "main": "dist/knockout-contrib-router-middleware-{{kebabCase name}}.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "WTFPL",
  "peerDependencies": {
    "@profiscience/knockout-contrib-router": "^{{getCurrentVersion 'router'}}",
    "knockout": "^3.5.0-beta"
  },
  "devDependencies": {
    "@profiscience/knockout-contrib-router": "^{{getCurrentVersion 'router'}}"
  },
  "scripts": {
    "build": "../../node_modules/.bin/taskr build -d ../..",
    "test": "DIR=$PWD; cd ../..; yarn test $DIR"
  }
}
